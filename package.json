{
  "private": true,
  "name": "zfast",
  "scripts": {
    "build": "pnpm run --parallel build",
    "clean": "esno scripts/clean.ts"
  },
  "devDependencies": {
    "@pnpm/workspace.find-packages": "^1.1.5",
    "@types/node": "^20.10.3",
    "commander": "^11.1.0",
    "esno": "^4.0.0",
    "fs-extra": "^11.2.0",
    "typescript": "^5.3.2"
  }
}
